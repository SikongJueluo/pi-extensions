{
  "name": "pi-extensions",
  "description": "Persional pi code agent extentsions",
  "scripts": {
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  },
  "type": "module",
  "private": true,
  "dependencies": {
    "@gotgenes/pi-permission-system": "^25.4.0"
  },
  "pi": {
    "extensions": [
      "./packages/pi-permission-inner-cmd/src/index.ts"
    ]
  }
}
