{
  "name": "pi-extensions",
  "description": "Persional pi code agent extentsions",
  "scripts": {
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "fallow": "fallow",
    "fallow:fix": "fallow fix --dry-run",
    "publish:packages": "pnpm -r publish --no-git-checks"
  },
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "private": true,
  "dependencies": {
    "@gotgenes/pi-permission-system": "^32.0.2"
  },
  "pi": {
    "extensions": [
      "./packages/pi-permission-inner-cmd/src/index.ts",
      "./packages/pi-permission-ai-judge/src/index.ts"
    ]
  },
  "devDependencies": {
    "fallow": "^3.25.0"
  }
}
